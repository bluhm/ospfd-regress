This collection of files will grow to a test suite for OpenBSD
ospfd.  The goal is to make it part of the OpenBSD src/regress
framework.  There the ospfd will run and get tested with crafted
OSPF packets and kernel routing messages.

For now, it is just bunch of experimental scripts.
